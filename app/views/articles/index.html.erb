<h1>Blog Posts </h1>

<% @articles.each do |post| %>
<div class="card card-lg bg-light p-5">
    <h3><%= post.title%></h3>
    <p> <%= post.body%><p>
    <%= link_to "Read More", api_v1_articles_path(post), :class => 'btn btn-secondary btn-lg active'%>
</div>
<%end%>